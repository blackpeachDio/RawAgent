tiktoken>=0.7.0
langchain-community
langchain-chroma
contextvars
langgraph
langchain-core
langchain
# LangGraph Redis checkpoint saver
langgraph-checkpoint-redis
# hybrid BM25+向量 RRF（rag/retrieval_pipeline EnsembleRetriever）
langchain-classic
streamlit
starlette>=0.37.0
uvicorn[standard]>=0.27.0
# 离线索引 PDF：复杂表格用 pdfplumber（rag/offline_index → utils.file_utils.pdf_loader）
pdfplumber>=0.11.0
# 混合检索 BM25（hybrid_bm25_enabled: true）
rank-bm25>=0.2.2

# MCP（Model Context Protocol）示例服务器依赖：用于学习 Cursor MCP 工具调用
mcp>=1.2.0,<2.0.0
click>=8.1.0
# Agent 通过 HTTP 连接远程 MCP；top_act_info_mcp 服务使用 FastMCP
langchain-mcp-adapters
fastmcp

# RAG 离线评测（可选）：python -m eval.ragas_rag_eval
# 建议 Python 3.11/3.12；Windows 需有对应 wheel，否则可能需 C++ Build Tools
# ragas
# datasets
