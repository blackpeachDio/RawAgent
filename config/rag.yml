chat_model_name: qwen-max
embedding_model_name: text-embedding-v4
# chat_model（含 Agent、RAG）的生成参数，经 model_kwargs 传给 DashScope
chat_temperature: 0
chat_max_tokens: 2048 # 模型输出最大token

# 轻量对话模型（默认 qwen-turbo）：查询改写、记忆注入分类、摘要等共用；各场景可在 agent.yml / chroma.yml 单独覆盖模型名
turbo_chat_model_name: qwen-turbo
