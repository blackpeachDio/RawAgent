# RawAgent 技能：raw_agent_skills/*/SKILL.md 正文在每次拼 system 时注入（见 tools.middleware.build_system_prompt）
# 与 Cursor 编辑器技能无关。

enabled: true
root: raw_agent_skills
# 每个技能正文（去 frontmatter 后）最大字符数
max_body_chars: 12000
# 所有技能注入块合计上限；0 表示不限制总长
inject_max_total_chars: 24000
